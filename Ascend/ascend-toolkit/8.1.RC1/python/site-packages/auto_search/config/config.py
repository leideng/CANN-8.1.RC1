#!/usr/bin/env python3
# -*- coding: UTF-8 -*-
# Copyright (c) Huawei Technologies Co., Ltd. 2022-2022. All rights reserved.
"""
rl schedule search, tss
"""
import datetime
import os
import resource

WORKSPACE = os.path.join(os.path.expanduser('~'), "ss_workspace")
FILE_PATH = os.path.dirname(os.path.realpath(__file__))
resource.setrlimit(resource.RLIMIT_CORE, (0, 0))

TIMEOUT = 200
TIMEOUT_HIGH_PERF = 1200
BASE_RUN_TIMEOUT = 600
RUN_TIMEOUT = 60
TUNE_TICK_THRESHOLD_MICROSECOND = 20


def _get_default_config():
    """

    :return:
    """
    return {
        "debug": False,
        "timeout": TIMEOUT,
        "op_name": "default",
        "TASK_ID": "single_search",
        "STORE_TMP_ID": datetime.datetime.now().strftime('%Y%m%d_%H%M%S_%f'),
        "lock_timeout": 600,
        "base_run_timeout": BASE_RUN_TIMEOUT,
        "run_timeout": RUN_TIMEOUT,
        "repeat_count": 20,
        "WORKSPACE": WORKSPACE,
        "error_tolerance": 0.0,
        "accuracy_tolerance": 0.001,
        "golden_input": [],
        "golden_output": [],
        "auto_schedule_golden": False,
        "auto_schedule_py": "",
        "redis_info": {},
        "timer": False,
        "init_evb": True,
        "stop_better_than_base": False,
        'at_axis_search': False,
        'tbe_compile_para': False,
    }


OPTION_CONFIG = _get_default_config()
